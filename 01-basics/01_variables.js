const mobile = 91311664059 // const can't be change 
let name = "ashu" 
var city = "champa" // don't use as scope problem is there agar loop me koi aur variable banaye same name ke toh wo har jagah change ho jayega

// console.table([to print as table])

//null is a standalone value of type object
//undefined ka type hi undefined hota hai